# Notebooks

This folder is intentionally kept minimal for the GitHub repo.

Recommended workflow:

- Run the scraper to populate `data/raw/`
- Run `scripts/clean_cars.py` to write `data/processed/cars_clean.parquet`
- Use your own local notebooks (not committed) to explore `data/processed/`

